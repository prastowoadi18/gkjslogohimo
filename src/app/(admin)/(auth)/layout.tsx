const ClerkLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="flex items-center justify-center h-[600px]">{children}</div>
  );
};

export default ClerkLayout;
